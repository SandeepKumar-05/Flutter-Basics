import 'package:flutter/material.dart';

class AppPallete {
  // Dark Theme Colors
  static const Color darkBackground = Color(0xFF0A0A0A); // Deep premium black
  static const Color darkSurface = Color(0xFF121212); // Slightly lighter black for cards/surfaces
  static const Color darkPrimaryText = Colors.white;
  static const Color darkSecondaryText = Colors.white70;

  // Light Theme Colors
  static const Color lightBackground = Colors.white;
  static const Color lightSurface = Color(0xFFF5F5F5); // Slightly gray for surfaces
  static const Color lightPrimaryText = Colors.black;
  static const Color lightSecondaryText = Colors.black87;

  // Shared Colors
  static const Color error = Colors.redAccent;
  static const Color transparent = Colors.transparent;
}
